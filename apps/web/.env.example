# Environment Variables Configuration
# Copy this file to .env.local for local development

# ===== DEVELOPMENT ENVIRONMENT =====
# Backend API URL - where your Rails/backend server is running
NEXT_PUBLIC_API_URL=http://localhost:3000

# Frontend URL - where your Next.js app is running (for server components)
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# ===== PRODUCTION ENVIRONMENT =====
# Backend API URL (production)
# NEXT_PUBLIC_API_URL=https://api.goodsongs.app

# Frontend URL (production)
# NEXT_PUBLIC_SITE_URL=https://www.goodsongs.app

# ===== NOTES =====
# 1. NEXT_PUBLIC_API_URL should point to your backend API server
# 2. NEXT_PUBLIC_SITE_URL should point to your Next.js frontend
# 3. For local development, make sure your backend is running on the specified port
# 4. Common backend ports:
#    - Rails: 3000 (default)
#    - Express: 3000, 8000, or 5000
#    - Django: 8000
#    - FastAPI: 8000
# 5. Next.js frontend runs on port 3001 (configured in package.json)